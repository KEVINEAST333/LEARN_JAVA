package LeetCode;
//给定两个非空二叉树 s 和 t，检验 s 中是否包含和 t 具有相同结构和节点值的子树。
public class Sloution3 {

/*
    Sloution2 sloution2 = new Sloution2();
    Sloution2 sloution3 = new Sloution2();
    public boolean isSubtree(Node s, Node t) {
        if(s == null) {
            return false;
        }
        return sloution2.isSameTree(s,t) || isSubtree(s.left,t) ||isSubtree(s.right,t);
    }
*/

}
